## Me:

```
同意你的推荐。ENTER PLAN MODE
```

## vscode with github copilot chat with sonnet 4.6:

```
[MODE: PLAN]

以下是完整的实施计划。 

Read [](file:///c%3A/Users/wubin/OOR/katas/commons-csv/dialogs)

---

## 实施清单

1. 在项目根目录 commons-csv 下创建 `demo\` 目录（作为独立 Maven 子项目根）

2. 在 `demo\` 下创建 pom.xml，内容包含：
    - `groupId`: `demo`，`artifactId`: `csv-demo`，`version`: `1.0-SNAPSHOT`
    - `packaging`: `jar`
    - `maven.compiler.source` / target 均为 `1.8`
    - `project.build.sourceEncoding`: `UTF-8`
    - 依赖：`org.apache.commons:commons-csv:1.14.1`（引用 Maven Central 稳定版，无需预先安装快照）
    - 配置 `maven-exec-plugin`（`exec:java`），主类设为 `demo.CsvDemo`

3. 创建目录 `demo\src\main\java\demo\`

4. 在 `demo\src\main\java\demo\` 下创建 `CsvDemo.java`，内容为：
    - 场景说明注释：模拟"员工花名册管理"场景
    - **写出阶段**：使用 `CSVFormat.DEFAULT.withHeader(...)` + `CSVPrinter`，将5条员工记录（工号、姓名、部门、薪资）写入当前目录下 `employees.csv` 文件，写完后打印"CSV 已写出到 employees.csv"
    - **读入阶段**：用 `CSVParser` 解析刚才生成的 `employees.csv`，按列名读取每条记录，逐行打印到控制台
    - **筛选阶段**：用 Java Stream 过滤出薪资 > 15000 的员工并打印，展示流式处理能力
    - 所有 IO 操作使用 `StandardCharsets.UTF_8`，异常向上抛出（`main` 声明 `throws Exception`）

5. 在 dialogs 目录下创建 `understand-the-java-code-2026-03-13--17-59.md`，内容分三个部分：
    - **第一部分——主要功能、特点、优势、劣势和适用场景**：
        - 主要功能：CSV 文件的读（解析）与写（格式化输出），支持多种方言格式
        - 特点：轻量零侵入、支持 12 种预定义格式方言、支持 SQL ResultSet 直接导出、支持 BOM/Unicode/emoji、支持流式处理
        - 优势：API 极简、文档完善、Apache 质量保证、Java 8+ 无额外学习成本
        - 劣势：仅处理 CSV/TSV，不支持 Excel `.xlsx` 格式（需用 Apache POI），不支持 CSV 和 Java 对象之间的自动映射（需自行编写或借助第三方如 OpenCSV）
        - 适用场景：数据导入导出、数据库数据备份、日志分析、ETL 管道、微服务之间的简单数据交换
    - **第二部分——如何在 Windows 11 PowerShell 7 中编译并运行演示应用**：
        - 前提条件检查命令（`java -version`、`mvn -version`），要求 JDK 8+ 和 Maven 3.6+
        - 步骤1：切换到 demo 目录：`cd C:\Users\wubin\OOR\katas\commons-csv\demo`
        - 步骤2：执行 `mvn compile`，Maven 自动从 Central 下载 commons-csv 1.14.1 依赖
        - 步骤3：执行 `mvn exec:java`，运行演示程序
        - 步骤4：说明预期控制台输出格式（3段：写出确认、读入表格、筛选结果）
        - 步骤5：说明 `employees.csv` 文件位置（`demo\` 目录下），可用记事本查看原始内容
    - **第三部分——如何运行自动化测试**：
        - 切换目录到项目根：`cd C:\Users\wubin\OOR\katas\commons-csv`
        - 仅运行测试（跳过静态检查）：`mvn clean test`
        - 说明测试数量和框架（JUnit 5，含单元测试、集成测试、参数化测试）
        - 完整检查命令（可选）：`mvn`（含 Checkstyle、SpotBugs、PMD、JaCoCo 覆盖率）
        - 说明测试报告位置：`target\surefire-reports\`

6. 在项目根目录 commons-csv 下创建 todo 文件 `todo-2026-03-13--17-59.md`，内容为上述编号清单（每项标注 `[ ]` 待实施状态）

---

下一步：

1. 进入下一模式：`ENTER EXECUTE MODE`
2. 继续讨论可复制：`请为AI自身制定工作计划，只列清单无需时长预估。将计划转为带编号清单，每项相互独立，创建时间戳文件，重新执行 PLAN 模式。`
```